# deckbuilder
sw deck builder
Before use open the cdf and set encoding to utf-8 in notepad++
Run python script from GIT shell in Windows
If printing lines out in cmd window, set it to utf-8 by running 'chcp 65001'
